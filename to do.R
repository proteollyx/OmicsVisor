# volcano plot printer does not refresh selected IDs
# Select only logFC and adjust adj.p-value selection accordingly without letting user change it
# Optional blue line and disclaimer that vector graphics can be edited according to needs
# explain for what donut plots can be useful
# ID list generator box size increase (layout issue)
# why does plotly not fix the label? - is this possible?
# explain on overview on how the format of the dataframe should be with the option of displaying an example
# explain why and how the tool is ID driven and how the IDs are built
# explain why it only works with this format -- not made for PTMs yet
# add a disclaimer why PCAs might non work with non-imputed data / NAs in the matrix

# add histograms for adj.p

# √ volcano select only comparison and automatically select logFC and adjP accordingly
# √ same for Volcano Printer Module
# √ copy IDs from left side, right side or both sides at once
# √ Select Label Columns: as in volcano plot module also for heatmap module (allows labelling with multiple columns as row identifer (heatmap))
# √ Donut, show number of IDs selected with the given cutoffs
# √ Boxplot/Violin Plot Module -- add the select all columns as well
# √ show jitter or beeswarm (mutually exclusive)


# how to cite?
# Select Row Name Column: doesnt have an effect any more
# login tool for stats
# option to impute for certain plots
# EasyPubPlot export functionality
# barplot or crossbar + points?

# LogFC Scatterplots
# QC
## All-by-all-correlation
## Gingras tSNE map
# histograms of adjusted p-values

# protein lists:
## crapome
## kinases
## common contaminants

# combine dataframes
