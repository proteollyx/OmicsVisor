# ── R session state ──────────────────────────────────────────────────────────
.Rhistory
.RData
.Rproj.user/
.Ruserdata
.Rapp.history

# .Rprofile is renv's activation hook and IS tracked; local overrides are not
.Rprofile.local

# ── renv (keep the lockfile, ignore the library) ─────────────────────────────
renv/library/
renv/staging/
renv/sandbox/
renv/local/
renv/python/

# ── OS / editor cruft ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*~
*.swp
.vscode/
.idea/

# ── Logs ─────────────────────────────────────────────────────────────────────
error logs/
*.log
*.Rout

# ── Deployment artefacts ─────────────────────────────────────────────────────
rsconnect/
manifest.json
.positai
.secrets

# ── Project / result data: never commit unpublished omics data ───────────────
data/
data-raw/
*QQ_Results*.xlsx
*.raw
*.mzML
*.mzXML
*.sqlite
*.rds
*.Rds
*.gct
*.gmt

# Test scratch output
tests/manual/*.tsv
tests/testthat/_snaps/*/*.new.*

# ── Temporary files ──────────────────────────────────────────────────────────
~$*
*.tmp

# ── Internal planning docs (not part of the public release) ──────────────────
v1.0_roadmap.md
v1.0_roadmap.html
